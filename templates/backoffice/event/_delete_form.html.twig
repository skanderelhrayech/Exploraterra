<form method="post" action="{{ path('app_evenement_delete', {'id': e.id}) }}" onsubmit="return confirm('Are you sure you want to delete this Event?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ e.id) }}">
    <button class="btn">Delete</button>
</form>
