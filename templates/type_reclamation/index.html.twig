{% extends 'base-back.html.twig' %}

{% block title %}TypeReclamation index{% endblock %}

{% block body %}
    <h1>TypeReclamation index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                 <th>Nom</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_reclamation in type_reclamations %}
            <tr>
                <td>{{ type_reclamation.id }}</td>
                <td>{{ type_reclamation.nom }}</td>
                <td>
                   <a href="{{ path('app_type_reclamation_show', {'id': type_reclamation.id}) }}" class="btn btn-info">Show</a>
                    <a href="{{ path('app_type_reclamation_edit', {'id': type_reclamation.id}) }}" class="btn btn-warning">Edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>
<a href="{{ path('app_type_reclamation_new')}}" class="btn btn-success">Create new</a>

   
{% endblock %}
